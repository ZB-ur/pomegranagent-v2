# Pomegranagent V2

这是独立开发的 V2。当前首要目标是：儿童用电脑自然讲述照顾小鸭的经历，并形成忠实、可回看的记录。教师可在旁协助，儿童也应能独立完成主要操作。产品背景和当前范围见 [docs/product.md](docs/product.md)，运行与验证方式见 [README.md](README.md)。

## 工作原则

- 从用户想完成的事组织设计和实现。先打通一条可体验的完整路径，再扩大功能范围。
- 在已确认目标内，自主选择和调整设计、技术方案与实现顺序。可逆的实现选择无需逐项确认；改变核心用户、产品范围、真实数据处理方式或对外发布时，先说明影响。
- 重视交互节奏、信息层级、中文表达与整体视觉品质。以实际运行的界面判断效果，允许推翻不合适的初稿。
- 用户已确认的交互与视觉方向见产品说明和可运行原型。实现可调整技术栈、代码结构和细节，但应保留已确认的使用路径与体验品质；原型中的模拟服务、存储方式和已知限制不是正式产品要求。
- 儿童识字能力有限：每个需要理解状态或采取行动的步骤，都必须有简短、可重听的中文语音引导和直观反馈，包含身份选择、开始/停止输入、等待回应、收尾保存、换人及出错恢复。儿童不核对文字，收尾播音后倒计时自动保存，由教师事后核对。按“不读屏幕文字也能继续”验收；逐状态要求见产品说明。空格键切换语音输入启停。常驻小鸭保持 V1 白鸭、圆框眼镜、暖黄色日记本的辨识特征，呈现温暖的视觉与状态动效。
- 儿童入口默认展示教师当天安排的一至多名幼儿；教师可从过去日期的排班进入补录，草稿按幼儿与活动日期隔离，以大头像选择身份；草稿与记录归属所选幼儿。儿童端核心流程可仅靠键盘完成，焦点可见且有语音引导。
- 首期必须接入真实对话 AI，理解幼儿本次语音输入与当前对话上下文，以简短中文语音回应并自然引导；一次最多一个问题，不诱导编造，不强制追问；默认 3 轮，教师可配置，末轮正常回应后接固定结束语。固定操作提示不能替代基于内容的 AI 接话。
- 首期支持 macOS 与 Windows，覆盖启动、中文听说、完整键盘操作、档案排班、记录持久化及备份恢复。技术选型兼顾两端；具体系统版本、架构与浏览器支持范围需明确，不能将单端通过视为双端通过。无法访问的平台如实列为待验收。
- 首期不新增单元测试，不采用 TDD，不搭建或扩充自动测试体系。验收以 Astra Computer Use 在实际应用中的操作结果及用户人工反馈为准；保留必要的构建、类型或语法检查与日志排查作为开发检查。
- 完成一条用户路径意味着：运行、亲自操作、检查结果、修复发现的阻塞，并准确说明仍未验证的部分。示例数据与模拟服务必须明确标识。
- 每次改动验证受影响的路径及相关联的关键行为，可交付阶段再走完整核心流程；未改动且已通过的页面不反复巡检。验收使用隔离的虚构资料。浏览器截图不能证明声音可听，模拟识别或模型不能证明真实服务可用；声音可懂度、真实儿童语音和环境适应性需用户设备听说验收。已有原型单测保留为历史参考，不扩充，也不作为首期完成门槛。
- 记录以儿童原话为依据。整理内容不得添加儿童没有表达的行动、情绪、原因或能力评价；保留原话以供教师核对。
- 保护真实数据和与当前任务无关的修改。旧项目只作参考；旧功能、架构、流程和项目指令需经当前需求判断后才采用。
- V1 相关记忆只提供历史线索，不能把旧版流程或功能自动变成 V2 要求。应用内对话、识别和播音服务需按产品需求选择，开发使用 Astra 不等于应用必须调用 Astra。

## 协作与知识

主智能体对整体产品体验和最终交付负责。后续开发中，请仅在独立任务的并行收益超过交接和审核成本时按需派发子智能体，无需逐次征求同意；通常不派发或只派发 1 个，小改动和需要连续产品判断的工作直接完成。派发时给出目标、必要上下文、写入边界和验收结果，避免重叠编辑；主智能体继续其他有用工作，并通过差异阅读与关键路径验收整合，不把子任务完整重做一遍。不设固定角色、难度打分表或逐步审查链。

以周限额内完成首期为预算目标，优先减少重复阅读、重复验收、无用功能与交接返工。主智能体默认 Astra / medium；架构取舍、语音生命周期、数据一致性和难定位阻塞按需使用 Astra / high。明确且独立的常规实现可试用 Sol / medium；核心交互判断与复杂状态仍交给 Astra。小任务主智能体直接完成。降级任务出现实质性理解偏差或反复返工时由 Astra 接手，后续同类任务不继续试错；暂不默认加入 Terra/Luna 或使用 xhigh/max/ultra。

上述分工是派发指导，实际模型和推理强度需通过客户端或工具支持的设置生效；无法切换时如实说明。以包含交接、审核和返工的总额度消耗判断收益，不能用 API 标价直接换算订阅周限额。每完成一个可体验阶段读取一次可用额度，结合实际进度调整；预留剩余额度约十分之一用于人工反馈后的修复与最终验收。额度为账号共享，无法据此保证固定工作量，预期超出时提前说明，不擅自削减已确认范围。

项目起步不增加自定义 skill。只有出现值得复用的专门知识或操作时再提取，保持触发条件明确、内容按需读取。稳定原则放在本文件，产品决定放在产品说明，实际命令和验证结果放在 README；避免重复维护。

新会话启动示例见 [docs/development-start.md](docs/development-start.md)。后续交接在 README 简要更新当前可运行入口、已验证路径和下一项阻塞，不维护重复的过程日志。
